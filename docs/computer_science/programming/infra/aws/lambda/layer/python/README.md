# python custom packages lambda layer 


# create layer zip
- run the command below in this directory.
- `python.zip` will be created.
```sh
$ docker-compose up \
  && docker-compose down --rmi=all
```